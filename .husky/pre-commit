#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Lancer lint-staged (linting + formatage des fichiers modifiés)
npx lint-staged

# Vérifier les types TypeScript
npm run type-check

echo "✅ Pre-commit checks passed!"
